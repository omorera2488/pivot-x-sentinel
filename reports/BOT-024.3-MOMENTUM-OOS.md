# BOT-024.3 — Validación OOS de Momentum en XAUUSDc

**US principal:** `BOT-024` (Signal Quality / Scoring 0–100) — no se cierra
ni se sustituye. **Esta ejecución:** `BOT-024.3`, sucesora de
[`BOT-024.2 — Momentum Feature Discovery`](BOT-024.2-MOMENTUM-FEATURE-DISCOVERY.md).

## Estado: `BOT-024.3 BLOCKED — insufficient OOS data`

No se ejecutó ningún análisis predictivo. No se generaron los CSV de
Universo A/B (no obligatorios en caso de bloqueo, por regla explícita del
enunciado). Producción permanece intacta.

---

## 1. Pre-flight — qué se confirmó (desde los datos, no de memoria)

### Dataset de discovery (BOT-024.2)

Releído directamente del archivo, no asumido:

| | |
|---|---|
| Archivo | `backtests/data/XAUUSDc_M5_latest.parquet` |
| Barras | 100 505 |
| Timestamp inicial | `2025-04-14T18:50:03+00:00` |
| Timestamp final (cutoff de discovery) | `2026-09-15T00:40:03+00:00` |

Coincide exactamente con lo documentado en BOT-024.2 (verificado con un
check explícito en el script, PASS).

### Dataset OOS disponible

Se buscó en `backtests/data/` cualquier archivo `XAUUSD*` con barras
estrictamente posteriores al cutoff de arriba, **sin descargar nada
nuevo** (regla explícita del enunciado):

| Archivo | Barras | Última barra | Barras posteriores al cutoff |
|---|---:|---|---:|
| `XAUUSDc_M5_20260915T004019Z.parquet` | 100 505 | `2026-09-15T00:40:03+00:00` | **0** |

Este archivo resultó ser **idéntico byte a byte** a `XAUUSDc_M5_latest.parquet`
(mismo tamaño, mismo contenido) — no es un segundo dataset, es el mismo
archivo con otro nombre. **No existe ningún otro archivo de precios XAU en
el repositorio.**

**Total de barras OOS encontradas: 0.**

### Decisión GO / BLOCKED

```text
BOT-024.3 BLOCKED -- insufficient OOS data

- Los datos disponibles en el repositorio llegan exactamente hasta
  2026-09-15T00:40:03+00:00 (mismo cutoff que uso BOT-024.2 para discovery).
- Barras OOS encontradas: 0.
- Por lo tanto: 0 LIMITS y 0 trades OOS evaluables -- no hay Universo A ni B posible.
- No se reutilizo el periodo de discovery como si fuera OOS (prohibido explicitamente).
- No se descargo ningun dato nuevo (requiere autorizacion explicita del usuario,
  no se asumio).
```

Ver [`reports/BOT-024.3-MOMENTUM-OOS-evidence.txt`](BOT-024.3-MOMENTUM-OOS-evidence.txt)
para el log completo y reproducible de esta verificación.

### Qué haría falta para desbloquear

Un histórico M5 real de `XAUUSDc` (o el símbolo que resuelva
`resolve_symbol("XAUUSD")` en el bróker conectado en ese momento) con
barras **estrictamente posteriores** a `2026-09-15T00:40:03+00:00`.

**Advertencia de tamaño de muestra, para cuando se decida desbloquear:** la
fecha actual (2026-09-19) implica, como mucho, ~4 días / ~1150 barras M5
disponibles si se descargara hoy. Con Config A generando aproximadamente 1
señal cada ~23 barras sobre las 100 505 de discovery, una ventana de pocos
días produciría del orden de 40-60 señales y bastante menos LIMITS/fills
tras el filtro de concurrencia — probablemente insuficiente para clasificar
la mayoría de H1-H6 como `REPLICATED`/`NOT REPLICATED` con confianza (se
esperaría `INSUFFICIENT DATA` en varias), aunque seguiría siendo el primer
dato real (no simulado) desde el cierre de discovery. Esta nota se deja
para cuando el usuario decida autorizar la descarga y/o esperar a acumular
más historial — no se tomó ninguna decisión al respecto en esta tarea.

---

## 2. Config A congelada (confirmada, no re-ejecutada)

Sin datos OOS no se ejecutó ningún backtest, pero se deja confirmada la
configuración que se usaría, tal como la fijó BOT-024.2 (idéntica a
BOT-042/043/045):

```text
ema_periods=12, periodos_htf_min=800, buf_bp=0.4, rr=1.0
max_concurrent_por_direccion=1, valid_bars=10, orden_viva=True,
max_bars_trade=500, fixed_lot=0.01, entrada_viva=False,
una_operacion_a_la_vez=True
```

No se optimizó, no se corrió grid search, no se probaron configuraciones
alternativas.

---

## 3. Hipótesis congeladas (H1–H6) — quedan pendientes, no evaluadas

Las hipótesis derivadas de BOT-024.2 (H1 Momentum corto/fill rate con
`roc_atr_3`; H2 Momentum corto/resultado condicional al fill; H3
`rsi_delta_3` como control de redundancia contra `roc_atr_3`, ρ≈0.935 en
discovery; H4 `ema_slope_atr_5`; H5 `atr_pct` como contexto de volatilidad;
H6 interacción Divergencia OPPOSED × Momentum fuerte, N=36/59 en discovery)
**se dejan tal cual están definidas en el enunciado de esta tarea, sin
modificar** — quedan congeladas para la próxima corrida de BOT-024.3 en
cuanto exista un dataset OOS real.

**Thresholds:** no se reconstruyeron los bin edges de los quintiles de
discovery en esta ejecución porque no hay ningún dato al que aplicarlos
todavía — reconstruirlos ahora sería trabajo especulativo no verificable.
Se reconstruirán desde `backtests/data/XAUUSDc_M5_latest.parquet` (nunca
desde OOS) en la próxima corrida, antes de aplicarlos a las barras nuevas.

---

## 4. Tabla final (obligatoria por el enunciado)

| Hypothesis | Discovery | OOS | N OOS | Classification |
|---|---|---|---:|---|
| ROC3 → Fill | fill rate decrece al aumentar momentum a favor (monotónico, Q1=85.5% vs Q5=68.0%, IC sin solape) | no evaluado | 0 | **INSUFFICIENT DATA** |
| ROC3 → Outcome (condicional a fill) | Q1 (más en contra) Exp$=−2.04, bootstrap CI excluye 0 | no evaluado | 0 | **INSUFFICIENT DATA** |
| RSI Δ3 redundancy vs ROC3 | ρ≈0.935 (Spearman) | no evaluado | 0 | **INSUFFICIENT DATA** |
| EMA slope 5 | patrón contraintuitivo (quintil "más limpio" peor, CI excluye 0, más marcado en LONG) | no evaluado | 0 | **INSUFFICIENT DATA** |
| ATR percentile | mayor correlación de screening (ρ≈0.37 vs pnl_r), forma no monotónica confirmada | no evaluado | 0 | **INSUFFICIENT DATA** |
| OPPOSED × Momentum fuerte | débil N=59 WR=49.2% Exp$=−2.95 vs fuerte N=36 WR=22.2% Exp$=−6.75 | no evaluado | 0 | **INSUFFICIENT DATA** |

Ninguna fila se convierte en scoring ni en decisión de diseño.

---

## 5. Respuestas explícitas a las preguntas del enunciado

1. **¿Se replica OOS la relación Momentum → Fill Rate?** No evaluable — sin
   datos OOS.
2. **¿Se replica OOS la relación Momentum → resultado una vez filled?** No
   evaluable.
3. **¿`roc_atr_3` y `rsi_delta_3` siguen siendo redundantes?** No evaluable.
4. **¿Hay evidencia para conservar `ema_slope_atr_5` como candidato
   independiente?** No evaluable.
5. **¿`atr_pct` aporta contexto reproducible o fue efecto de discovery?** No
   evaluable.
6. **¿La interacción OPPOSED × Momentum fuerte vuelve a aparecer?** No
   evaluable.
7. **¿Los efectos son similares en LONG y SHORT?** No evaluable.
8. **¿Cuál de los hallazgos de BOT-024.2 sobrevivió fuera de muestra?**
   Ninguno pudo evaluarse todavía — el repositorio no contiene ninguna
   barra fuera de la muestra de discovery al momento de esta ejecución.
9. **¿Hay evidencia suficiente para avanzar a una definición formal de
   Momentum, o hace falta acumular más OOS?** Se necesita acumular datos
   OOS reales antes de poder responder esto — no se puede avanzar a una
   definición formal sin al menos una validación fuera de muestra.

---

## 6. Producción intacta

```text
$ git status --porcelain
?? reports/BOT-024.3-MOMENTUM-OOS-evidence.txt
?? reports/BOT-024.3-MOMENTUM-OOS.md
?? scripts/validate_momentum_oos_xau.py
(mas los artefactos de BOT-024.2, ya reportados en esa tarea)

$ git diff --stat -- strategy/ execution/ api/ panel/
(sin salida -- sin diferencias)
```

No se implementó `MomentumScore`, no se asignaron pesos, no se activó
gating, no se integró D1, no se modificó `BOT-025`.

---

## 7. Archivos de esta tarea

- [`scripts/validate_momentum_oos_xau.py`](../scripts/validate_momentum_oos_xau.py) — script de pre-flight/detección de OOS (nuevo, solo lectura). Vuelve a ejecutarse sin cambios cuando se quiera reintentar esta tarea; detecta automáticamente si ya existe un dataset OOS.
- [`reports/BOT-024.3-MOMENTUM-OOS-evidence.txt`](BOT-024.3-MOMENTUM-OOS-evidence.txt) — log reproducible de la verificación de bloqueo.
- Este documento.
- No se generaron CSVs de Universo A/B (no obligatorio en caso de bloqueo).

**Siguiente paso:** re-ejecutar `scripts/validate_momentum_oos_xau.py` en
cuanto exista un dataset XAU con barras posteriores a
`2026-09-15T00:40:03+00:00` — si detecta barras OOS, implementar en ese
momento la Parte 2 (validación H1-H6 con thresholds congelados de
discovery, reusando el patrón de shadow replay + paridad exhaustiva de
BOT-024.2) contra datos reales, no antes.
